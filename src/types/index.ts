export type RegimeAndJurisdiction = {
    tax: string,
    taxRegimeCode: string,
    taxRateCode: string,
    taxStatusCode: string,
    taxJurisdictionCode: string,
    providerRecRate?: number,
    providerRecRateCode?: string,
}

